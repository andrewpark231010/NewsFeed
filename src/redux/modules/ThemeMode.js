const initState = {
  mode: 'light',
};

const ThemeModeReducer = (state = initState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
};

export default ThemeModeReducer;
